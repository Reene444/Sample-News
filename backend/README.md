### Relative Middleware 

1. **Install Apicache**:
   ```bash
   npm install apicache
  Applied in router: [index.js](./src/routes/index.js).

  ***Reference*** - [Apicache on npm](https://www.npmjs.com/package/apicache)
2. **Using the Cluster Module**：

The `cluster` module is a built-in module in Node.js, so we can use it directly 

Applied in server js file: [www.js](./bin/www).

***Reference*** - [Cluster on npm](https://www.npmjs.com/package/cluster)

